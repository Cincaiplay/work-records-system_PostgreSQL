<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title><%= title || "Access denied" %></title>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" />
  <link rel="stylesheet" href="/css/styles.css" />
</head>

<body class="bg-light">
  <%- include("partials/navbar", { active: (typeof active !== "undefined" ? active : null) }); %>

  <div class="container my-5">
    <div class="row justify-content-center">
      <div class="col-12 col-lg-8 col-xl-7">

        <div class="card shadow-lg border-0 rounded-4 overflow-hidden">
          <div class="card-body p-4 p-md-5">

            <div class="d-flex align-items-start gap-3">
              <div class="rounded-3 bg-danger-subtle text-danger d-flex align-items-center justify-content-center"
                   style="width: 52px; height: 52px;">
                <i class="bi bi-shield-lock fs-3"></i>
              </div>

              <div class="flex-grow-1">
                <h1 class="h4 fw-bold mb-1">403 — Access denied</h1>
                <p class="text-muted mb-3">
                  You’re signed in, but your account doesn’t have permission to view this page.
                </p>

                <% if (typeof missingPermission === "string" && missingPermission) { %>
                  <div class="alert alert-warning rounded-3 mb-3">
                    <div class="fw-semibold mb-1">Missing permission</div>
                    <code><%= missingPermission %></code>
                  </div>
                <% } %>

                <% if (typeof message === "string" && message) { %>
                  <div class="text-muted small mb-3"><%= message %></div>
                <% } %>

                <div class="d-flex flex-wrap gap-2">
                  <a href="/" class="btn btn-primary rounded-pill px-4">
                    <i class="bi bi-house-door me-1"></i> Go Home
                  </a>

                  <button type="button" class="btn btn-outline-secondary rounded-pill px-4" onclick="history.back()">
                    <i class="bi bi-arrow-left me-1"></i> Go Back
                  </button>
                </div>

                <hr class="my-4" />

                <div class="text-muted small">
                    If you believe this is a mistake, contact an admin and include:
                    <div class="mt-2">
                        <code><%= (typeof missingPermission === "string" && missingPermission) ? missingPermission : "N/A" %></code>
                    </div>
                </div>


              </div>
            </div>

          </div>
        </div>

      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <script src="/js/company-switcher.js"></script>
</body>
</html>
